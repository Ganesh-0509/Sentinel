# --- SentinelAI core spine (Phase 1: simulate -> model -> scoreboard) ---
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
lightgbm>=4.3
matplotlib>=3.8
joblib>=1.3

# --- Phase 2 (explainability + anomaly + rules) ---
# shap>=0.45

# --- Phase 3 (RAG + agents) ---
# faiss-cpu>=1.8
# langchain>=0.2
# langgraph>=0.1
# google-generativeai>=0.7      # Gemini (primary LLM)
# ollama>=0.3                    # local fallback LLM (offline demo safety)
# sentence-transformers>=3.0     # embeddings (BGE) if not using hosted embeddings

# --- Phase 4 (API + serving) ---
# fastapi>=0.111
# uvicorn>=0.30
# pydantic>=2.7
